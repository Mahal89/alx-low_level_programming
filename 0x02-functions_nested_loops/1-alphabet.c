#include <stdio.h>
#include "Mahal.h"
/**
 *main - entry point
 *Description: loops
 *Return: always 0
*/
int main(void)
{
void print_alphabet(void);
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
putchar('\n');
return (0);
}
