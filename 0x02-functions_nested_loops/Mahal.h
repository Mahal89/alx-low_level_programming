#include <stdio.h>
char print_alphabet(char ch);
int main(void)
{
char ch = 'a';
char = print_alphabet (ch);
putchar(ch);
return (0);
}
char print_alphabet (ch)
{
char result;
result = (ch = 'a'; ch <= 'z'; ch++)
return result;
}
