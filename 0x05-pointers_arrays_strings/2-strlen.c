#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 *_strlen - prints length of string
 *@s: points at the string
 *Return: 0
 */
int _strlen(char *s)
{
char *s;
int length = strlen(s);
return (length);
}
