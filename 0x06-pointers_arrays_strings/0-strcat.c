#include <stdio.h>
#include "main.h"
/**
 *strcat - concatenates strings
 *@dest: first string
 *@src: second string
 *Return: return dest
*/
char *_strcat(char *dest, char *src);
{
char s1[100], s2[100];
int length; k;
length = 0;
while (s1[length] != '\0')
{ 
++length;
}
for (k = 0; s2[j] != '\0', ++j, ++length)
{
s1[length] = s2[j];
}
s1[length] = '\0';
return (dest);
}
