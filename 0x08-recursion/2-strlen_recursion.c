#include <stdio.h>
#include "main.h"
/**
 *_strlen_recursion - prints length
 *@s: character
 *Return: 0
*/
int _strlen_recursion(char *s)
{
int k;
if (*s)
{
_strlen_recursion(k = 0; s[k] != '\0', k++)
_putchar("%d", k);
}
}
      
