#include <stdio.h>
#include "main.h"
/**
 *_sqrt_recursion - entry point
 *@n: argument
 *Return: return -1 if n has no square root
*/
int _sqrt_recursion(int n)
{
int k;
k = sqrt;
if (n < 0 && n / k = double)
{
return (-1);
}
else
{
_sqrt_recursion(n);
}
return (0);
}
