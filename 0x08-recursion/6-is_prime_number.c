#include <stdio.h>
#include "main.h"
/**
 *is_prime_number - function
 *@n: argument
 *Return: 1 if prime number,otherwise 0
*/
int is_prime_number(int n)
{
int i;
n = n / i;
if (n < 2 ||  n % 2 = 0 || (i != 1 && i != n))
{
return (0);
}
else
{
return (1);
}
}
